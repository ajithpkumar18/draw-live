import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Excalidraw - Collaborative Drawing Made Simple',
  description: 'Transform your thoughts into beautiful hand-drawn diagrams. Excalidraw makes it simple to create, share, and collaborate on visual ideas in real-time.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}